<nav>
  <a href="/">Home</a>
  <a href="/books">Books</a>
  <a href="/cart">Cart</a>

  {{#if user}}
    <span style="margin-left:12px; opacity:.85;">{{user.email}}</span>
    <form method="POST" action="/auth/logout" style="display:inline;">
      <button class="btn secondary" type="submit" style="margin-left:10px;">Logout</button>
    </form>
    {{#if (eq user.role "admin")}}
      <a href="/books/new" style="margin-left:10px;">Add Book</a>
      <a href="/orders" style="margin-left:10px;">Orders</a>
    {{/if}}
  {{else}}
    <a href="/auth/login" style="margin-left:10px;">Admin Login</a>
  {{/if}}
</nav>
